#include <cstdio>

char[100050] directions;
char[100050] sign;

int main() {
    int length;
    while (scanf("%d", &length), length != 0) {
        int x = 0;
        int y = 0;
        int z = 0;

        for (int i = 0; i < length - 1; i++) {
            char sign, direction;
            scanf("%c%c", &sign, &direction);

            
        }
    }
}